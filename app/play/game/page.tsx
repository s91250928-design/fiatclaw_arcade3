"use client";

/**
 * Full-screen vault game route.
 * Entered from lobby PLAY NOW — not embedded in dashboard.
 */
import { GameSession } from "@/components/game/GameSession";

export default function PlayGamePage() {
  return <GameSession />;
}
